# P0 改造方案：让 PPT-AGENT 同时好看且可编辑

## 这是什么

这份文档描述 `PPT-AGENT` 的 P0 架构目标：保留 `HTML-first` 主链路，同时把可编辑导出、回读审计和模板一致性拉到一个更稳的层级。

## 为什么要这么做

只要 HTML 端和 editable 端不是同一套设计约束，最终就会出现两类问题：

- 图片版看起来完整，但 editable 版像被翻译过一遍
- editable 结构能改，但视觉层级、留白和布局密度失真

## 为什么这是好主意

P0 不追求一次性重写全链路，而是优先加固最影响结果质量的几个节点，投入小，回报直接。

## 当前主链路

```text
outline -> content -> plan -> step4_html
       -> build_pptx(image)
       -> build_dom_editable_deck_from_html
       -> audit / preview / manifest
```

## P0 核心模块

1. 迁移内核
   负责统一模板、设计约束、图片规划和 HTML 生成。
2. DOM editable 导出器
   负责把 HTML 页面回放成对象级 PPTX。
3. 结构与回读审计
   负责检查导出结果是否退化成截图壳，或在 Office 回读时明显漂移。
4. 渲染安全层
   负责在高密布局下自动收紧字号、间距和 chip 区安全距离。

## 当前落地原则

- 先保证导出稳定，再追求样式复杂度
- 先保证对象级可编辑，再追求百分百视觉复刻
- 先收敛公共渲染安全层，再修个别页面死数据

## 当前完成项

- 已接入迁移内核的 HTML 生成入口
- 已接入 DOM editable 导出
- 已接入 PowerPoint 回读预览与差异审计
- 已为高密度卡片补充渲染安全样式
- 已去除仓库内旧品牌文案与相关产物残留

## 下一步

- 扩大回放样本，继续验证历史产物
- 评估是否还需要继续压缩少数高密度页面的标题区与 footer 安全间距
